<!doctype html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Document</title>
	<style>
		*{
			margin: 0;padding: 0;
			list-style: none;
		}
		.box{
			margin: 0 auto;
			width: 800px;
			height: auto;
		}
		p{
			font-size: 10px;
			color:#f00;
			cursor:pointer;
		}
	</style>
</head>
<body>
	<div class="box">
		<h3>李泊的博客</h3>
		<p class="btn">点击添加文本内容</p>
		<ul>
			<%data.forEach(function(i){%>
				<li><span class="no"><%=i.no%></span><%=i.tit%></li>
			<%})%>
		</ul>
	</div>
	<script src="./jquery.js"></script>
	<script>
	$(".btn").on("click",function(){
		window.location.href="/text"
	})
		$("li").on("click",function(){
			var name=$(this).text()
			var no=$(this).children(".no").text()
			$.ajax({
				url:"/index",
				type:"post",
				data:{no:no},
				success:function(){
					window.location.href="/list?no="+no
				}
			})
			$.ajax({
				url:"/counter",
				type:"post",
				data:{no:no},
				success:function(){
					if(msg=='ok'){
					window.location.href="/list?no="+no
						
					}
				}
			})
		})
	 </script>
</body>
</html>
